# Experimental seed directory (public build)

No experimental history is bundled in the public/GitHub distribution.
Use **Record Lab Data** or **Import Lab Data** to add data you are authorized to use.
The resulting runtime database is user-local and must not be committed to Git.
